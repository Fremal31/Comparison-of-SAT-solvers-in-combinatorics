[tool.pytest.ini_options]
testpaths = ["tests"]

pythonpath = ["src"]

markers = [
    "integration: marks tests as integration tests requiring solver binaries (deselect with -m 'not integration')"
]

[tool.mypy]
python_version = "3.9"
mypy_path = "src"
explicit_package_bases = true
strict = true